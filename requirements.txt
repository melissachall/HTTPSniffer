# HTTP Sniffer MITM - Requirements
# Educational Network Analysis Tool
# Python 3.8+
# Author: Melissa Hall (@melissachall)
# Date: 2025-11-23

# =====================================================
# CORE DEPENDENCIES
# =====================================================

# Packet manipulation and network sniffing
scapy>=2.5.0

# Colorized terminal output
colorama>=0.4.6

# =====================================================
# SYSTEM DEPENDENCIES (install via apt)
# =====================================================
# Run these commands before pip install:
#
# sudo apt update
# sudo apt install -y python3 python3-pip
# sudo apt install -y dsniff (provides arpspoof)
# sudo apt install -y tcpdump (optional, for debugging)
# sudo apt install -y net-tools (optional, for ifconfig)
#
# =====================================================

# =====================================================
# OPTIONAL DEPENDENCIES
# =====================================================
# These are not required but useful for development:
#
# pylint>=2.15.0          # Code linting
# black>=22.10.0          # Code formatting
# pytest>=7.2.0           # Testing framework
#
# =====================================================

# =====================================================
# NOTES
# =====================================================
#
# Installation:
#   pip3 install -r requirements.txt
#
# If scapy fails to install:
#   sudo apt install python3-scapy
#
# For HTTP layer support, ensure scapy version >= 2.5.0
#
# Enable IP forwarding before use:
#   sudo sysctl -w net.ipv4.ip_forward=1
#
# =====================================================
